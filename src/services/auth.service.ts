/*
 * Business Logic & DB Queries (The worker)
 * - Check if user exists
 * - Hash the password using `bcrypt`
 * - Preparing `JWT`
 * - Save the user on database using `prisma`
 * - Filter the data
 * - Return the data to `controllers`
 */
