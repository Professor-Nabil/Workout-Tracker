> [!NOTE] Security NOTE
> storing long-lived refresh tokens in localStorage
> exposing your application to Cross-Site Scripting (XSS) attacks.
> If a malicious script runs on your site,
> it can steal those tokens and compromise user accounts.

```bash
# HACK: (XSS) attacks
```
